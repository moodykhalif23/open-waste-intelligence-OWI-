import logging

logger = logging.getLogger(__name__)


def run_inference(observation_id: str) -> None:
    """Placeholder until Phase 1 models land — the queue contract is live from day one."""
    logger.info("inference queued for observation %s (no models deployed yet)", observation_id)
